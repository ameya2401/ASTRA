"""
src/api/main.py - FastAPI application entrypoint.

Creates and configures the FastAPI app instance, registers
routers, and manages application lifespan events (database
initialization, model loading).

Implementation: Phase 5
"""
