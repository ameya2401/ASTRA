# AST-Triage: Project Context

> **Last Updated:** Phase 4 - Calibrated Classifier & SHAP Attribution  
> **Date:** September 20, 2026

---

## What is AST-Triage?

AST-Triage is an **automated risk-scoring system** designed to evaluate Pull Requests generated by AI coding agents (such as Devin, Claude Code, GitHub Copilot, OpenHands, etc.). It determines how likely a given PR is to be defective, misaligned with the original issue, or unsafe to merge, before a human reviewer inspects it.

### The Problem It Solves

AI coding agents are increasingly generating pull requests autonomously. However, these PRs can contain:
- **Semantic drift**: the code does not actually solve the described issue
- **Test cheating**: deleted assertions or blanket try/except blocks that mask failures
- **Structural bloat**: excessive complexity changes without clear necessity
- **Scope creep**: touching files unrelated to the original task

AST-Triage catches these patterns automatically by analyzing the PR code at three levels:

1. **Structural (AST):** Tree-sitter parses syntax trees to detect cyclomatic complexity changes, node mutations, and signature breaks
2. **Semantic (NLP):** Sentence-transformers compare the issue description against the actual diff to detect intent misalignment
3. **Statistical (ML):** A Platt-calibrated XGBoost classifier combines 28 engineered features into a single calibrated risk probability

---

## Current Status

### ✅ Phase 1: Project Scaffolding & Environment Setup - COMPLETE

What was accomplished:
- **Dependencies**: `requirements.txt` with core libraries (FastAPI, tree-sitter, XGBoost, sentence-transformers, SQLAlchemy, etc.)
- **Configuration**: Pydantic BaseSettings in `config/settings.py` loading from `.env` with GitHub tokens, DB URL, model paths, and risk thresholds
- **Database Models**: SQLAlchemy 2.0 async ORM with 4 tables (`repositories`, `pull_requests`, `feature_records`, `triage_predictions`)
- **Full Directory Skeleton**: All `__init__.py` files and placeholder stubs for every module
- **Database Connection**: Async engine factory with SQLite support, session management, and table initialization
- **Data & Model Directories**: `data/raw/`, `data/processed/`, `data/fixtures/`, `models/`

### ✅ Phase 2: Tree-sitter AST Differencing Engine - COMPLETE

What was accomplished:
- **`src/ast_engine/parser.py`**: Tree-sitter Python grammar wrapper with pre-configured parser, node type constants, and text extraction helpers
- **`src/ast_engine/cyclomatic.py`**: McCabe Cyclomatic Complexity calculator and maximum nesting depth calculator
- **`src/ast_engine/differ.py`**: Structural AST comparison engine computing all 12 features (F01-F12)
- **Multi-file aggregation**: `diff_multi_file()` aggregates metrics across multiple changed files
- **38 unit tests**: all passing in `tests/test_ast_engine.py`

### ✅ Phase 3: Intent-to-Diff Semantic Alignment Engine - COMPLETE

What was accomplished:
- **`src/semantic_engine/embedder.py`**: Thread-safe singleton wrapper around `SentenceTransformer('all-MiniLM-L6-v2')`
- **`src/semantic_engine/drift_analyzer.py`**: NLP feature extraction engine computing all 6 features (F13-F18)
- **Text & Diff Normalization**: Sanitizes markdown, HTML, and unified diff headers
- **Entity Extraction**: Regex identification of code identifiers and Jaccard distance calculation
- **34 unit tests**: all passing in `tests/test_semantic_engine.py`

### ✅ Phase 4: Calibrated Classifier & SHAP Attribution - COMPLETE

What was accomplished:
- **`src/feature_pipeline/vector_builder.py`**:
  - Full 28-D vector constructor with schema mapping and alias resolution
  - Shannon dispersion entropy calculation (`calculate_dispersion_entropy`)
  - Test churn extraction (`extract_test_metrics`) and process provenance extractor (`extract_process_metrics`)
- **`src/feature_pipeline/normalizer.py`**:
  - `FeatureNormalizer` with bound clipping and missing value imputation (`impute_missing_values`, `clip_feature_bounds`)
- **`src/ml_engine/explainer.py`**:
  - `TreeSHAPExplainer` wrapper for `shap.TreeExplainer`
  - Positive risk driver extraction and human-readable formatting (e.g. `assertions_deleted = 3 (+0.32 SHAP)`)
- **`src/ml_engine/trainer.py`**:
  - XGBoost training pipeline configured with `scale_pos_weight` for 4:1 class imbalance
  - 5-fold Platt calibration via `CalibratedClassifierCV(method='sigmoid', cv=5)`
  - Model evaluation computing ROC-AUC, PR-AUC, F1, and Brier Score
  - Serialization to `models/calibrated_triage_xgb.joblib` and `models/shap_explainer.joblib`
  - Synthetic dataset generator (`generate_synthetic_pr_dataset`)
- **`src/ml_engine/predictor.py`**:
  - Thread-safe singleton `RiskPredictor` runtime with sub-15ms latency
  - Risk tier classification (LOW < 0.25, MEDIUM [0.25, 0.65), HIGH >= 0.65)
  - Hard safety override checks (semantic drift flag, assertion deletion anomaly, disturbance with failing CI)
- **`scripts/train_model.py`**: Standalone training and verification CLI script
- **19 unit tests**: all passing in `tests/test_feature_pipeline.py` (91 total tests passing across Phases 2, 3, and 4)

### ⬜ Phase 5: FastAPI Webhook Gateway & GitHub Reporter - PENDING

---

## How the System Will Work (End-to-End)

```
GitHub PR Event -> FastAPI Webhook -> AST Parser -> Semantic Embedder -> Feature Vector (28-D) -> XGBoost -> Risk Score + SHAP -> GitHub Comment
```

1. A GitHub webhook fires when an AI agent opens or synchronizes a PR
2. FastAPI ingests the PR payload (diff, issue description, metadata)
3. Tree-sitter parses pre/post code into ASTs and computes structural deltas
4. Sentence-transformers embed the issue and diff, computing cosine similarity
5. 28 features are assembled into a vector
6. XGBoost predicts a calibrated defect probability
7. SHAP explains the top 3 risk-driving features
8. A formatted risk report is posted back to the GitHub PR

---

## Key Design Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Database | SQLAlchemy 2.0 async + SQLite | Async for FastAPI compatibility; SQLite for zero-config dev, swappable to Postgres |
| Config | Pydantic BaseSettings | Type-safe, `.env` loading, validation at startup |
| AST Parsing | Tree-sitter (C bindings) | Fast, language-agnostic, production-grade syntax analysis |
| Embeddings | all-MiniLM-L6-v2 | 384-dim, fast inference, good quality for short-text similarity |
| Classifier | XGBoost + Platt calibration | High accuracy on tabular data; calibration ensures probabilities reflect empirical defect rates |
| Explainability | SHAP TreeExplainer | Local feature attribution, explaining to maintainers why a PR is risky |
